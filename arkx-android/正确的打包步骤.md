# 📦 Android 打包 - 正确步骤

## ⚠️ 重要：ArkUI X 的正确创建方式

根据官方文档，ArkUI X 项目的创建和打包流程如下：

## 🚀 第一步：创建 ArkUI X 项目

### 1. 安装 ArkUI-X SDK（如果还没有）

1. 打开 DevEco Studio
2. 导航至 **File > Settings > ArkUI-X**（macOS 为 **DevEco Studio > Preferences > ArkUI-X**）
3. 点击 `Location` 右侧的 `Edit`
4. 选择 SDK 存储路径
5. 完成 SDK 安装

### 2. 创建 ArkUI-X 工程

1. **File > New > Create Project**
2. 在**模板市场**中，选择 **`[ArkUI-X]Empty Ability`**（不是 ArkUI X > Empty Template）
3. 点击 **Next**
4. 填写工程配置：
   - Project name: `StepSportsAndroid`
   - Save location: 选择保存位置
5. 点击 **Finish**，等待模板工程创建完成

## 📋 第二步：复制迁移的代码

```bash
# 复制所有 ETS 代码到新项目
cp -r arkx-android/src/main/ets/* <新项目路径>/entry/src/main/ets/

# 复制页面路由配置
cp arkx-android/main_pages.json <新项目路径>/entry/src/main/resources/base/profile/main_pages.json

# 复制应用配置
cp arkx-android/app.json5 <新项目路径>/AppScope/app.json5
```

## 🔨 第三步：生成 Android 工程

1. 在 DevEco Studio 中打开 ArkUI X 项目
2. 点击主菜单栏：**Build > Build Hap(s)/APP(s) > Build APP(s)**
3. 等待编译完成
4. 编译后的代码会生成到 Android 和 iOS 应用工程中

生成的 Android 工程通常位于：
```
<项目路径>/android/
```

## 📱 第四步：在 Android Studio 中打包

1. **打开 Android Studio**
2. **打开生成的 Android 工程**
   - File > Open
   - 选择 `<项目路径>/android/` 目录
3. **同步项目**
   - 点击 "Sync Project with Gradle Files"
4. **构建 APK**
   - Build > Generate Signed Bundle / APK
   - 选择 **APK**
   - 配置签名
   - 选择 **release**
   - 点击 **Finish**

## 📍 APK 输出位置

构建完成后，APK 文件位于：
```
android/app/build/outputs/apk/release/app-release.apk
```

## ⚠️ 重要说明

1. **DevEco Studio** 用于 ArkUI X 源码的开发和调试
2. **Android Studio** 用于 Android 平台的 Native 代码开发和打包
3. **Xcode** 用于 iOS 平台的 Native 代码开发（如果需要）

## 🔍 如果找不到 [ArkUI-X]Empty Ability 模板

### 检查 DevEco Studio 版本
- 确保使用**最新版本**的 DevEco Studio
- ArkUI X 功能需要较新的版本支持

### 检查插件
- File > Settings > Plugins
- 搜索 "ArkUI X" 或 "ArkUI-X"
- 确保插件已安装并启用

### 更新 SDK
- File > Settings > SDK
- 检查是否有 ArkUI X SDK
- 如果没有，需要更新 DevEco Studio

## 🐛 常见问题

### Q: 找不到 [ArkUI-X]Empty Ability 模板
A: 
1. 更新 DevEco Studio 到最新版本
2. 检查是否安装了 ArkUI X 插件
3. 尝试 File > Settings > SDK 更新 SDK

### Q: Build APP(s) 选项是灰色的
A: 
1. 确保项目是 ArkUI X 项目
2. 检查项目配置是否正确
3. 尝试先 Build > Rebuild Project

### Q: 生成的 Android 工程无法打开
A: 
1. 确保 Android Studio 已安装
2. 检查 Android SDK 是否配置正确
3. 查看构建日志中的错误信息

## 📚 参考文档

- [华为开发者文档 - ArkUI X](https://developer.huawei.com/)
- [ArkUI X 开发指南](https://developer.harmonyos.com/)

---

**总结流程**：
1. DevEco Studio 创建 `[ArkUI-X]Empty Ability` 项目
2. 复制代码
3. Build > Build APP(s) 生成 Android 工程
4. Android Studio 打开生成的工程并打包 APK

